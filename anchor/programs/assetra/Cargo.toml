[package]
name = "assetra"
version = "0.1.0"
description = "Assetra: pre-IPO index vault (Frontier Index) with on-chain Auto-Invest"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "assetra"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build", "demo_desk/idl-build"]
anchor-debug = []
custom-heap = []
custom-panic = []

[dependencies]
anchor-lang = { version = "1.0.2", features = ["init-if-needed"] }
anchor-spl = "1.0.2"
demo_desk = { path = "../demo_desk", features = ["cpi"] }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_os, values("solana"))'] }
